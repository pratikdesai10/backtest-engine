# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Python-based backtesting engine for financial/trading strategies. Licensed under Apache 2.0. Currently in early development on the `feature/build-backtest-engine` branch.

## Repository Status

This is a greenfield project. No source code, build system, dependencies, or tests have been set up yet. The `.gitignore` is pre-configured for Python development (virtual environments, **pycache**, IDE files, build artifacts).
